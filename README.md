# Import Sort: the only sort worse than bubble sort

To run:

    python -B mergesort.py
    
The `-B` flag is critical because it prevents module caching, which screws up the dynamic module importing.